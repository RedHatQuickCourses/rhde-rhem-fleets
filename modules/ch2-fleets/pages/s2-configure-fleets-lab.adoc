:time_estimate: 20

= Lab: Configure and Manage Device Fleets

_Estimated reading time: *{time_estimate} minutes*._

Objective::

Configure multiple device fleets and assign devices to fleets using labels and selectors.

WARNING: Pending Review

== Before you begin

You need a _development machine_ running RHEL, and hosting a few local VMs which stand in for managed devices.
These local VMs are already provisioned and enrolled in Edge Manager.

You also need a _server machine_ running RHEL, which is already configured as an Edge Manager server.

Both your _development machine_ and _server machine_ were configured by performing the xref:ch1-scale:s3-environment-lab.adoc[first lab].

If you are using the course classroom, your _development machine_ is the `workstation` VM.
Log in as the user `student` with password `student`.
From that machine, you may start SSH sessions to the `servera` VM, which is your _server_ machine, using the same user.
Alternatively, you can open a shell or a console directly on the `servera` VM.

== Instructions

You will create fleets, using both the Edge Manager web UI and its CLI, and put devices on those fleets.
You will also switch devices between different fleets, and observe how those changes affect managed device configurations.
For simplicity, you will use only inline configurations.

include::ch1-scale:partial$set-permissive.adoc[]

1. On your _developer machine_, verify that you have the prerequisites from the xref:ch1-scale:s3-environment-lab.adoc[first lab].
If any of the prerequisites are missing, please return to the first lab and ensure that you did not skip any step.

.. Verify that you have a clone of the course samples repository.
+
[source,console,subs="verbatim,quotes"]
--
$ *cd*
$ *ls rhde-rhem-samples*
antora.yml  config  enroll ...
--

.. Verify that you have two local VMs.
+
[source,console,subs="verbatim,quotes"]
--
$ *virsh list --all*
 Id   Name     State
------------------------
 1    edge-1   running
 2    edge-2   running
--

.. If your VMs are not running, start them, but ensuring they are booting from power on instead of resumed from saved state.
+
[source,console,subs="verbatim,quotes"]
--
$ *virsh list --all*
 Id   Name     State
-------------------------
 -    edge-1   shut off
 -    edge-2   shut off
--
+
[source,console,subs="verbatim,quotes"]
--
$ *virsh start edge-1 --force-boot*
Domain 'edge-1' started
$ *virsh start edge-2 --force-boot*
Domain 'edge-2' started
--

.. Give your local VMs a few moments to finish boot.
Then, log in on Edge Manager as a non-administrator and verify that there are two managed devices, which are on-line and up-to-date.
+
[source,console,subs="verbatim,quotes"]
--
$ *flightctl login -u student -p student https://servera.lab.example.com:3443*
Auto-selected organization: 00000000-0000-0000-0000-000000000000 Default
Login successful.
--
+
[source,console,subs="verbatim,quotes,attributes"]
--
$ *flightctl get device*
NAME                                                    ALIAS   OWNER   SYSTEM  UPDATED         APPLICATIONS
{edge-2-uuid-bbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbb}    edge-2  <none>  Online  UpToDate       NoApplications
{edge-1-uuid-aaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaa}    edge-1  <none>  Online  UpToDate       NoApplications
--

.. If you devices keep displaying as off-line for too long, it might be related to clock skew.
In that case, stop each of your local VMs and return to the beginning of this lab.
+
NOTE: Clock skew prevents a device from synchronizing with Edge Manager, and is known to happen if your ROLE classroom is stopped without first shutting down local VMs on your _develoment machine_ and later your VMs are restarted from saved state.
+
[source,console,subs="verbatim,quotes"]
--
$ *virsh destroy edge-1*
Domain 'edge-1' destroyed
$ *virsh destroy edge-2*
Domain 'edge-2' destroyed
--

2. Create a device fleet, using the web UI.
The process is very similar to editting a device configuration, if you remember the activity from the first course.
+
You will create a fleet with a device template that sets two configuration files, using the inline configuration provider: A pre-login banner (an "issue" message), and a lifecycle hook that enables login using the serial console.

.. On your _development machine_, start a web browser and open `\https://servera.lab.example.com`.
Log in on Edge Manager as `student` using the `student` password.
On the left pane, click menu:Fleets[].
It should display a page which states "No fleets yet".

.. Click menu:Create fleet[]. 
Name your new fleet as `store` and, under *Device selector*, click on menu:Add label[].
Type `location=store` as the device selector, and press kbd:[Tab] to leave the text box. 
Ignore the warning that "There are no devices matching these labels" and click menu:Next[].

.. Under *Host configuration (files)*, click menu:Add configuration[] and type `store` on the *Source name* field. 
Select `Inline configuration` as the *Source type* field.

.. Under *File 1*, type `/etc/issue.d/warning.issue` as the *File path* field, and type the following on the *Content* field:
+
[source,console,subs="verbatim,quotes"]
--
This is managed device {{ .metadata.labels.alias }}
Access by non-authorized personnel is strictly forbidden.
--
+
Also ensure that there is a blank line at the end of the multi-line text box.

.. Scroll down and click menu:Add file[] to create *File 2*.
Type `/etc/flightctl/hooks.d/afterupdating/enable-serial-console.yaml` as the *File path* field, and type the following on the *Content* field:
+
[source,console,subs="verbatim,quotes"]
--
- run: /usr/bin/systemctl enable serial-getty@ttyS0.service --now
--

.. Click menu:Next[] until you reach the end of the *Create fleet* assistant and click menu:Create fleet[].
You should get a details page for a fleet in the `Valid` state and with no devices.

3. Alter the labels on the first managed device so it joins the fleet and verify it got the fleet configuration.

.. Switch to a terminal and attempt to open the serial console of your local VM.
It does not display a log in prompt, no matter which keys you press.
Your only alternative is dettaching from the VM console using kbd:[Ctrl+\]].
+
[source,console,subs="verbatim,quotes,macros"]
--
$ *virsh console edge-1*
Connected to domain 'edge-1'
Escape character is ^] (Ctrl + ])
kbd:[Enter]
kbd:[Ctrl+\]]
--

.. Back to your web browser, click menu:Devices[] on the left pane.
Then click on the menu icon (three vertical dots) for the `edge-1` managed device and select menu:Edit device configurations[].

.. Click on menu:Add label[] and type `location=store` on the text box.
Press kbd:[Tab] to close the text box and notice that the page displays a green check mark under *Fleet name*, acknowledging that the new label makes the device a member of the `store` fleet.

.. Click menu:Next[] until you reach the last page of the assistant, and click menu:save[].

.. Scroll down and notice that the *Update status* is `Out-of-date`.
Wait until it changes to `Up-to-date`.

.. Switch to a terminal and attempt again to open a terminal.
You may have to press kbd:[Enter] a few times to get a log in prompt.
After you get the prompt, press kbd:[Ctrl+D] to reset the log in process and view the banner messages.
+
You should see the standard banner from Edge Manager, which displays a QR code used to enroll or manage the device, and the warning that the fleet configuration added.
+
You do not need to log in.
You can press kbd:[Ctrl+\]] to dettach from the virtual console, after confirming the warning banner is displayed by the device.
+
[source,console,subs="verbatim,quotes,attributes,macros"]
--
$ *virsh console edge-1*
kbd:[Enter]
enroll login: 
kbd:[Ctrl+D]kbd:[Enter]
Red Hat Enterprise Linux 9.6 (Plow)
Kernel 5.14.0-570.76.1.el9_6.x86_64 on an x86_64 (ttyS0)

enp1s0: 172.25.250.9 fe80::5054:ff:fec1:c7f

█████████████████████████████████████████████████████████
█████████████████████████████████████████████████████████
████ ▄▄▄▄▄ █▀ ▀██▀▄▀▀▄█▀▄  ▀▄  ▄▀ ▄ ██  █▄▀▀ █ ▄▄▄▄▄ ████
████ █   █ █▀▀▀ ▀▄▄▀▀█▀▀▄█▄▄ ▄ ▄▄██  ▄▄ ▀▄█ ▄█ █   █ ████
████ █▄▄▄█ █▄▄█▄█▀▀█ ▄ ▀▄▀ ▄▄▄ ██▀▄██ ██ ▄▀███ █▄▄▄█ ████
████▄▄▄▄▄▄▄█ █ ▀ ▀▄▀ █ █ █ █▄█ █▄▀ ▀ █▄█ █ ▀▄█▄▄▄▄▄▄▄████
████▀▄▀  ▄▄█▄▀█▀▄ ▀  ▀▀ ▀ ▄▄▄▄▄█▄▀▀█��▀ ▄█ ▀▀▀▄ █  █ █████
█████▀▄▄▀ ▄▀▄▀  ▀█▀  ▀▀▀ ▄ ▄█▀  ▀▄▀ ▀▄ ███▀▀██▀█   ▀▄████
████▄▀▀ ▀ ▄██▄██▄  ▄██▀▄ █▄▄▀ ▀▀▄▄▀▀ ▄▄█ �� ▄█▀ ▀▄█▀█▄████
████▀█▀█▀█▄  ▄▀▄ █ ▀ █▀▀ ▀▄███  ▄▄▀ ██▀ ▄▄ ▄█▀▀▀▄ ▀ ▄████
████▀██ █▄▄▀▄▀ ▄▄   ▄▄▄  ▀▀█ ▄█▀▄▀ █▄█▀▀ ▄▄ █   ▄▄▄▀█████
████ ▄▄▀▀▄▄ █▀▄▀█ ▀▀  ▀▀█ ▀▄█▀ █ █ ▀▄█▄ ▄█▄ ▀█▀█▄ ▄▀ ████
█████▄▀ ▀▄▄▀▄▄█ ▄ ▀█ ▄▀ ▄█ ▀█ ██▄▄ ██ ▀▄ █ █▀▀ █▀█▀█▄████
████   �� ▄▄▄ ▄▄█ ▄███▀ ▀██ ▄▄▄ ▄ ▄▀  █  ▄██▀ ▄▄▄ ▄ █▄████
████▄ ▀  █▄█ ▄█ ██▄▀██▄ ██ █▄█ █▀▄ ▄ ▀█  ▄ █ █▄█  ▀██████
████ █▄▄▄▄▄▄▄▀▀▀▀��▀▀ █▄ ▀▀▄▄ ▄ ▀█▄▀▀▄▄▀▄▄ ▄ ▄ ▄  ▀ ▄ ████
█████ ▄ ▄█▄▀▄▄███▀▀▀██ ▄▄ ▄  ▀█▄▀█▀ ▄█ █▄ █▄▄ ▀▀█   ▄████
████  ▀ ▀▄▄█▄▀▀▄ ▄▀█▄ ▀█▀ ██��   ▄▄██ ▀▀  ▄▀  █▄▀▄▀▄▀▄████
██████▄ ▄█▄█▄▀█ ██ ▄▀ ▄█  █ ▀██  █  ▀ ▀▀ █▀ █▀ ▀ ▀▀█▄████
████▀ ▀▄██▄██▀  ▄▄ ▄▀▄ █  ▀█▀▀  ██▀▀   ▄▄ █ █▀���██▄▀█ ████
████  █▄▀ ▄ ██▀▀  █    █▀█▀█▄   ▄▀ █▄▀█ ▄▀▀▄█ ▀▀▀█▀█▄████
█████ ▀▀█▄▄█▀▀▀▄▀▀███▀█▄▄  ▀▀██ █▀█▄█▀█ ▄▀▄ ▀▀▄▄▀ ▀▀▄████
████▄▄▄███▄▄▀█ ▀█ ▄▄▀▄ █▀▄ ▄▄▄ █▀▄█▄▄ ▄▀ ▀▀  ▄▄▄    ▄████
████ ▄▄▄▄▄ █▀▄ ▄ █▄  ▀██ ▄ █▄█ ▀  ▄██ ▄▀▄▄▄█ █▄█  ▄█ ██��█
████ █   █ █ █ ▄▄▀█ ▄█   █ ▄ ▄ ▄▄▀▀▀▀ ▀  ▀ ▄   ▄▄▀█ ▀████
████ █▄▄▄█ █▀▀ ▄  ▀▄▀▀█▄▀▄▀ █▄  ▄▄▀█▀ ▄▀█ ▀▄▀   ▄  ▀ ████
████▄▄▄▄▄▄▄██████▄▄▄▄▄███▄██▄▄█▄▄▄▄█▄███▄▄▄▄▄██▄▄▄██▄████
█████████████████████████████████████████████████████████
▀▀▀▀▀▀▀▀���▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀

Your device is enrolled to flightctl,
you can manage your device scanning the above QR. or following this URL:
\https://servera.lab.example.com:443/manage/{edge-1-uuid-aaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaa}

This is managed device edge-1
Access by non-authorized personnel is strictly forbidden.
enroll login:
kbd:[Ctrl+\]]
--

4. Configure the second managed device, using the CLI, so you can later see the impact on its previous configuration, when that device joins a fleet.

.. Create a temporary working directory, and copy example files to it.
+
[source,console,subs="verbatim,quotes"]
--
$ *mkdir temp-fleet*
$ *cd temp-fleet*
$ **cp -r ../rhde-rhem-samples/fleet/* .**
--

.. Review the device manifest.
It configures a post-login banner (a "message of the day").
+
[source,console,subs="verbatim,quotes"]
--
$ *cat edge-2-motd.yaml*
include::1@samples:fleet:example$edge-2-motd.yaml[]
--

.. Replace the device UUID on the device manifest with the actual UUID of your managed device.
+
[source,console,subs="verbatim,quotes,attributes"]
--
$ *UUID=$( flightctl get device -l alias=edge-2 -o name )*
$ *echo $UUID*
{edge-2-uuid-bbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbb}
$ *sed -i "s|REPLACE_WITH_UUID|$UUID|" edge-2-motd.yaml*
--

.. Apply the device manifest to update the device configuration.
+
[source,console,subs="verbatim,quotes,attributes"]
--
$ *flightctl apply -f edge-2-motd.yaml*
edge-2-motd.yaml: applying device/{edge-2-uuid-bbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbb}: 200 OK
--

.. Wait until the device status changes to `Up-to-Date`.
+
[source,console,subs="verbatim,quotes,attributes"]
--
$ *flightctl get device $UUID* -o wide
NAME                                                    ALIAS   OWNER   SYSTEM  UPDATED         APPLICATIONS    LABELS
{edge-2-uuid-bbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbb}    edge-2  <none>  Online  UpToDate        NoApplications  type=noapps,alias=edge-2,rhel=9.6
--

.. Open an SSH connection to the device, verify that it displays the post-login banner, and terminate the SSH connection.
+
[source,console,subs="verbatim,quotes"]
--
$ *ssh -i ../temp-enroll/edge-key -p 8222 core@127.0.0.1*
Under new management!
Last login: Wed Mar 11 19:50:57 2026 from 172.25.250.254
[core@enroll ~]$ *exit*
logout
Connection to 127.0.0.1 closed.
--

.. Attempt to access the virtual console of the device, and verify that you can't get a log in prompt.
Press kbd:[Enter] a few times and dettach from the VM using kbd:[Ctrl+\]].
+
[source,console,subs="verbatim,quotes,macros"]
--
$ *virsh console edge-2*
Connected to domain 'edge-2'
Escape character is ^] (Ctrl + ])
kbd:[Enter]
kbd:[Ctrl+\]]
--

5. Join the device to the fleet, using the CLI, and verify its configuration changes.

.. Edit the device manifest to add the label `location=store` to its metadata.
+
[source,console,subs="verbatim,quotes,attributes"]
--
$ *flightctl edit device $UUID*
apiVersion: flightctl.io/v1beta1
kind: Device
metadata:
  ...
  labels:
    alias: edge-2
    rhel: "9.6"
    type: noapps
    *location: store*
  name: {edge-2-uuid-bbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbb}
  ...
spec:
  config:
  - inline:
    - content: Under new management!
...
device/{edge-2-uuid-bbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbb} edited
--

.. Wait until the device status changes to `Up-to-Date`.
Notice that the device now displays as belonging to a fleet: see column `OWNER`.
+
[source,console,subs="verbatim,quotes,attributes"]
--
$ *flightctl get device $UUID -o wide*
NAME                                                    ALIAS   OWNER           SYSTEM  UPDATED         APPLICATIONS    LABELS
{edge-2-uuid-bbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbb}    edge-2  Fleet/store     Online  UpToDate        NoApplications  location=store,rhel=9.6,type=noapps,alias=edge-2
--

.. Open an SSH connection to the device, verify that its post-login banner is gone, and terminate the SSH connection.
+
[source,console,subs="verbatim,quotes"]
--
$ *ssh -i ../temp-enroll/edge-key -p 8222 core@127.0.0.1*
Last login: Wed Mar 11 20:06:24 2026
[core@enroll ~]$ exit
logout
Connection to 127.0.0.1 closed.
--

.. Access the virtual console of the device, and verify that the pre-login banner displays after the QR code.
Press kbd:[Enter] a few times to get a log in prompt and press kbd:[Ctrl+D] to initialize a new log in process and see the banners.
You do not need to log in, just dettach from the VM using kbd:[Ctrl+\]].
+
[source,console,subs="verbatim,quotes,attributes,macros"]
--
$ *virsh console edge-2*
Connected to domain 'edge-2'
Escape character is ^] (Ctrl + ])
kbd:[Enter]
enroll login: 
kbd:[Ctrl+D]kbd:[Enter]
Red Hat Enterprise Linux 9.6 (Plow)
...

Your device is enrolled to flightctl,
you can manage your device scanning the above QR. or following this URL:
\https://servera.lab.example.com:443/manage/{edge-2-uuid-bbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbb}

This is managed device edge-2
Access by non-authorized personnel is strictly forbidden.
enroll login:
kbd:[Ctrl+\]]
--

6. Provision a third managed device, and set its labels so it joins the fleet at enrollment.

.. By now, you have one fleet with two devices.
+
[source,console,subs="verbatim,quotes,attributes"]
--
$ *flightctl get fleet*
NAME    OWNER   SELECTOR        VALID
store   <none>  location=store  True
$ *flightctl get device -o wide*
NAME                                                    ALIAS   OWNER           SYSTEM  UPDATED         APPLICATIONS    LABELS
{edge-2-uuid-bbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbb}    edge-2  Fleet/store     Online  UpToDate        NoApplications  type=noapps,alias=edge-2,location=store,rhel=9.6
{edge-1-uuid-aaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaa}    edge-1  Fleet/store     Online  UpToDate        NoApplications  alias=edge-1,location=store,rhel=9.6,type=noapps
--

.. To list only devices from a given fleet, use a field selector.
+
[source,console,subs="verbatim,quotes,attributes"]
--
$ *flightctl get device --field-selector metadata.owner=Fleet/store*
NAME                                                    ALIAS   OWNER           SYSTEM  UPDATED         APPLICATIONS
{edge-2-uuid-bbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbb}    edge-2  Fleet/store     Online  UpToDate        NoApplications
{edge-1-uuid-aaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaa}    edge-1  Fleet/store     Online  UpToDate        NoApplications
--

.. Use the script in the `temp-enroll` directory, from the first lab, to provision a third test VM.
Dettach from its console when VM creation is complete by pressing kbd:[Ctrl+\]].
+
[source,console,subs="verbatim,quotes"]
--
$ *cd ../temp-enroll*
$ *bash virt-install.sh 3*
Creating VM edge-3, use port 8322 for SSH.
...
Domain creation completed.
Restarting guest.
Running text console command: virsh --connect qemu:///session console edge-3
Connected to domain 'edge-3'
Escape character is ^] (Ctrl + ])
--

.. Log in on Edge Manager as an administrator and approve the enrollment request, but setting the `location` label to match the fleet.
+
[source,console,subs="verbatim,quotes,attributes"]
--
$ *flightctl get enrollmentrequest*
NAME                                                    APPROVAL        APPROVER        APPROVED LABELS
{edge-2-uuid-bbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbb}    Approved        admin           alias=edge-1,rhel=9.6,type=noapps
{edge-3-uuid-cccccccccccccccccccccccccccccccccccccc}    Pending         <none
{edge-1-uuid-aaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaa}    Approved        admin           alias=edge-1,rhel=9.6,type=noapps
--
+
[source,console,subs="verbatim,quotes,attributes"]
--
$ *flightctl login -u admin -p redhat https://servera.lab.example.com:3443*
Auto-selected organization: 00000000-0000-0000-0000-000000000000 Default
Login successful.
$ *flightctl approve enrollmentrequest -l alias=edge-3 -l location=store _{edge-3-uuid-cccccccccccccccccccccccccccccccccccccc}_*
--

.. Verify that the new device joined the fleet.
+
[source,console,subs="verbatim,quotes,attributes"]
--
$ *flightctl get device --field-selector metadata.owner=Fleet/store*
NAME                                                    ALIAS   OWNER           SYSTEM  UPDATED         APPLICATIONS
{edge-2-uuid-bbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbb}    edge-2  Fleet/store     Online  UpToDate        NoApplications
{edge-3-uuid-cccccccccccccccccccccccccccccccccccccc}    edge-3  Fleet/store     Unknown OutOfDate       Unknown
{edge-1-uuid-aaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaa}    edge-1  Fleet/store     Online  UpToDate        NoApplications
--

.. Wait until your third managed device is `UpToDate`.
+
[source,console,subs="verbatim,quotes,attributes"]
--
$ *UUID=$( flightctl get device -l alias=edge-3 -o name )*
$ *flightctl get device $UUID*
NAME                                                    ALIAS   OWNER           SYSTEM  UPDATED         APPLICATIONS
{edge-3-uuid-cccccccccccccccccccccccccccccccccccccc}    edge-3  Fleet/store     Online  UpToDate        NoApplications
--

.. Verify that the new device displays a pre-login banner, proving it alredy contains the fleet's configurations.
Press kbd:[Enter] to get a log in prompt and press kbd:[Ctrl+D] to initialize a new log in process and see the banners.
Then dettach from the VM using kbd:[Ctrl+\]].
+
[source,console,subs="verbatim,quotes,macros"]
--
$ *virsh console edge-3*
Connected to domain 'edge-3'
Escape character is ^] (Ctrl + ])
kbd:[Enter]
enroll login: 
kbd:[Ctrl+D]kbd:[Enter]
Red Hat Enterprise Linux 9.6 (Plow)
...
This is managed device edge-3
Access by non-authorized personnel is strictly forbidden.
enroll login:
kbd:[Ctrl+\]]
--

7. Create a second fleet, using the CLI, and move a device to it, so we can see how it affects configs originally from the previous fleet.

.. Return to the working directory of this lab, and log in on Edge Manager as a non-administrator, to maintain a good security posture.
+
[source,console,subs="verbatim,quotes"]
--
$ *cd ../temp-fleet*
$ *flightctl login -u student -p student https://servera.lab.example.com:3443*
Auto-selected organization: 00000000-0000-0000-0000-000000000000 Default
Login successful.
--

.. Review the fleet manifest.
It configures a custom prompt for shells in managed devices.
+
[source,console,subs="verbatim,quotes"]
--
$ *cat warehouse.yaml*
include::1@samples:fleet:example$warehouse.yaml[]
--

.. Create your second fleet using the manifest.
+
[source,console,subs="verbatim,quotes"]
--
$ *flightctl apply -f warehouse.yaml*
warehouse.yaml: applying fleet/warehouse: 201 Created
--

.. Edit the manifest for the `edge-2` device and change its `location` label to match the selector from the `warehouse` fleet.
+
[source,console,subs="verbatim,quotes,attributes"]
--
$ *UUID=$( flightctl get device -l alias=edge-2 -o name)*
$ *flightctl edit device $UUID*
apiVersion: flightctl.io/v1beta1
kind: Device
metadata:
  ...
  labels:
    alias: edge-2
    *location: warehouse*
    rhel: "9.6"
    type: noapps
  name: {edge-2-uuid-bbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbb}
...
device/{edge-2-uuid-bbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbb} edited
--

.. Wait until the device status changes to `Up-To-Date`.
+
[source,console,subs="verbatim,quotes,attributes"]
--
$ *flightctl get device $UUID*
NAME                                                    ALIAS   OWNER           SYSTEM  UPDATED         APPLICATIONS
{edge-2-uuid-bbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbb}    edge-2  Fleet/warehouse Online  UpToDate        NoApplications
--

.. Open a SSH connection to the device and verify that its prompt has changed.
+
[source,console,subs="verbatim,quotes"]
--
$ *ssh -i ../temp-enroll/edge-key -p 8222 core@127.0.0.1*
Last login: Wed Mar 11 20:16:11 2026 from 172.25.250.254
managed device: edge-2$ *exit*
logout
Connection to 127.0.0.1 closed.
--

.. Attach to the virtual console of the device and verify that it does show a log in prompt, but does not show the pre-login banner from `store` fleet anymore.
Remember to press kbd:[Enter] to get the login prompt, and to press kbd:[Ctrl+D] to display the banners.
Then press kbd:[Ctrl+\]] to detach from the console.
+
[source,console,subs="verbatim,quotes,attributes,macros"]
--
$ *virsh console edge-2*
Connected to domain 'edge-2'
Escape character is ^] (Ctrl + ])
kbd:[Enter]
enroll login: 
kbd:[Ctrl+D]kbd:[Enter]
Red Hat Enterprise Linux 9.6 (Plow)
Kernel 5.14.0-570.76.1.el9_6.x86_64 on an x86_64
...
Your device is enrolled to flightctl,
you can manage your device scanning the above QR. or following this URL:
\https://servera.lab.example.com:443/manage/{edge-2-uuid-bbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbb}

enroll login:
kbd:[Ctrl+\]]
--
+
[NOTE]
====
The `serial-getty` service is still up, enabling console logins, because its Systemd unit was enabled and started, by the `store` fleet configuration, from a hook.
The hook file itself was deleted by switching fleets, but the Systemd unit is still enabled and running and will keep that way after reboot.

This example illustrates how using imperative configurations may require extra care when updating configurations using Edge Manager. 
====

8. Create a third fleet, which overlaps with the first or second fleet.

.. Review the fleet manifest.
Notice that its selector is the same as the `store` fleet.
+
[source,console,subs="verbatim,quotes"]
--
$ *cat kiosk.yaml*
include::1@samples:fleet:example$kiosk.yaml[]
--

.. Create the fleet.
+
[source,console,subs="verbatim,quotes"]
--
$ *flightctl apply -f kiosk.yaml*
kiosk.yaml: applying fleet/kiosk: 201 Created
--

.. The status of the fleets provide *no hint* that there is a conflict, despite the duplicate selectors.
The status of the devices provide no hint either.
+
[source,console,subs="verbatim,quotes,attributes"]
--
$ *flightctl get fleet*
NAME            OWNER   SELECTOR                VALID
kiosk           <none>  location=store          True
store           <none>  location=store          True
warehouse       <none>  location=warehouse      True
$ *flightctl get device*
NAME                                                    ALIAS   OWNER           SYSTEM  UPDATED         APPLICATIONS
{edge-2-uuid-bbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbb}    edge-2  Fleet/warehouse Online  UpToDate        NoApplications
{edge-3-uuid-cccccccccccccccccccccccccccccccccccccc}    edge-3  Fleet/store     Online  UpToDate        NoApplications
{edge-1-uuid-aaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaa}    edge-1  Fleet/store     Online  UpToDate        NoApplications
--

.. The conflict is only noted in the conditions of the affected devices.
+
[source,console,subs="verbatim,quotes,attributes"]
--
$ *UUID=$( flightctl get device -l alias=edge-1 )*
$ *flightctl get device $UUID -o yaml*
apiVersion: flightctl.io/v1beta1
kind: Device
...
  labels:
    alias: edge-1
    location: store
    rhel: "9.6"
    type: noapps
  name: {edge-1-uuid-aaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaa}
  owner: Fleet/store
...
  - lastTransitionTime: "2026-03-12T16:20:25.862755686Z"
    message: kiosk,store
    reason: MultipleOwners
    status: "True"
    type: MultipleOwners
...
--

.. The following script uses the `jq` command to filter only devices with the `MultipleOwners` condition, if any.
+
[source,console,subs="verbatim,quotes,attributes"]
--
$ *cat get-devices-multipleowners.sh*
include::1@samples:fleet:example$get-devices-multipleowners.sh[]
$ *bash get-devices-multipleowners.sh*
Device: kml2gjca0l19lq7v1hqj9od3emin53u58skj76me4ue1772n0s40
Labels: {"alias":"edge-3","location":"store"}
---
Device: mpr39vjcj5fup095de4pqnld6h4snt2ud79g74p7spv5i00jjid0
Labels: {"alias":"edge-1","location":"store","rhel":"9.6","type":"noapps"}
---
--
+
NOTE: The web UI displays a warning icon on all devices having this and other conditions, making it easier to spot issues with devices.
Unfortunately, the web UI does not display a warning on the fleets themselves.

.. There is also a script that just count the number of devices with the condition.
+
[source,console,subs="verbatim,quotes,attributes"]
--
$ *cat count-devices-multipleowners.sh*
include::1@samples:fleet:example$count-devices-multipleowners.sh[]
$ *bash count-devices-multipleowners.sh*
2
--
+
// coundn't get those filters to work as a long command and display them properly for cut-and-paste from Antora.

.. Fix the fleet manifest by chaging its selector.
Open the `kiosk.yaml` file with a text editor and make the following changes:
+
[source,subs="verbatim,quotes"]
--
apiVersion: flightctl.io/v1beta1
kind: Fleet
metadata:
  name: kiosk
spec:
  selector:
    matchLabels:
      *location: parking lot*
  template:
...
--

.. Update the fleet.
+
[source,console,subs="verbatim,quotes"]
--
$ *flightctl apply -f kiosk.yaml*
kiosk.yaml: applying fleet/kiosk: 200 OK
--

.. Verify that now all your fleets have different selectors, and that no device has the `Multiple Owners` condition.
+
[source,console,subs="verbatim,quotes"]
--
$ *flightctl get fleet*
NAME            OWNER   SELECTOR                VALID
kiosk           <none>  location=parking lot    True
store           <none>  location=store          True
warehouse       <none>  location=warehouse      True
--
+
[source,console,subs="verbatim,quotes"]
--
$ *bash count-devices-multipleowners.sh*
0
--

10. Remove a device from its fleet, so you can see the impact on the configurations it got from its fleet.

.. Edit the manifest for device `edge-1` to remove its `location` label.
+
[source,console,subs="verbatim,quotes,attributes"]
--
$ *UUID=$( flightctl get device -l alias=edge-1 -o name )*
$ *flightctl edit device $UUID*
apiVersion: flightctl.io/v1beta1
kind: Device
metadata:
  ...
  labels:
    alias: edge-1
    rhel: "9.6"
    type: noapps
  name: {edge-1-uuid-aaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaa}
  owner: Fleet/store
...
device/{edge-1-uuid-aaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaa} edited
--

.. Verify that the device does not belong to any fleet, and wait until it is `Up-to-Date`.
+
[source,console,subs="verbatim,quotes,attributes"]
--
$ *flightctl get device $UUID -o wide*
NAME                                                    ALIAS   OWNER   SYSTEM  UPDATED         APPLICATIONS    LABELS
{edge-1-uuid-aaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaa}    edge-1  <none>  Online  UpToDate        NoApplications  type=noapps,alias=edge-1,rhel=9.6
--

.. Verify that the device still contains the pre-login banner set by its old fleet.
+
[source,console,subs="verbatim,quotes"]
--
$ *ssh -i ../temp-enroll/edge-key -p 8122 core@127.0.0.1 -- cat /etc/issue.d/warning.issue*
This is managed device edge-1
Access by non-authorized personnel is strictly forbidden.
--

.. Verify that the device manifest contains a copy of its old fleet configuration.
+
[source,console,subs="verbatim,quotes,attributes"]
--
$ *flightctl get device $UUID -o yaml*
apiVersion: flightctl.io/v1beta1
kind: Device
...
  labels:
    alias: edge-1
    rhel: "9.6"
    type: noapps
  name: {edge-1-uuid-aaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaa}
  resourceVersion: "212"
spec:
  applications: []
  config:
  - inline:
    - content: |
        This is managed device edge-1
        Access by non-authorized personnel is strictly forbidden.
      path: /etc/issue.d/warning.issue
    - content: '- run: systemctl enable serial-getty@ttyS0.service --now'
      path: /etc/flightctl/hooks.d/afterupdating/enable-serial-console.yaml
    name: store
status:
...
--

You demonstrated that Edge Manager keeps track of the configurations it puts on managed devices, as they join and switch fleets.
You also demonstrated that, when a device switches to no fleet, it keeps its last configuration.
Devices also keep their last applied configuration in case there's a conflict and they end up belonging to multiple fleets.

== What's next

Now that you have organized your devices into fleets, the next chapter explores how to fetch operating system configurations from a git repository, instead of having them inline inside a device or fleet manifest.

////
UPDATE: tasks related to git split to the next lab.

Configure fleets and assign devices to fleets. Need at least two fleets and a number of devices on each fleet. Have preseeded fleet configurations in a git repository, so learners can just refer to them. Show that, once a device belongs to a fleet, no configuration cannot be set at the device level. All configurations must come from the fleet. The changes, at an individual device level, from joining a fleet are essentially the same as if a device were updated or repurposed.
////
